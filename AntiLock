--// Created By Nosssa supports mostly, all hood games!
 
--// Roblox Group ( TeamNosss! ): https://www.roblox.com/groups/16003304/TeamNosss#!/about

getgenv().X_Axis_Offset = 0 -- Left And Right | Horizontal (X) -- Leave on default if you don't understand

getgenv().Y_Axis_Offset = -999 -- Up And Down | Vertical (Y) -- Leave on default if you don't understand

getgenv().Z_Axis_Offset = 0 -- Forward and Backwards | Depth (Z) -- Leave on default if you don't understand

getgenv().AntiLockKeybind = (  "x"  )

getgenv().UnResolvableMode = false -- "true" means the upper 3 options do nothing and the anti-lock just goes WILD!

loadstring(game:HttpGet("https://raw.githubusercontent.com/Nosssa/NossLock/main/My600lbLife", true))();
